Public v1.0-public verification record
Verification date: 2026-08-19
Environment: Python 3.12.13; dependencies pinned in requirements-lock.txt

Command:
PYTHONDONTWRITEBYTECODE=1 MPLCONFIGDIR=/tmp/ptm45-mpl-final \
  python -m unittest discover -s tests -v

Coverage:
- model metadata and enforced SHA-256
- repository-relative netlist paths and current-sign convention
- VGS sweep geometry and wrdata parsing
- minimum-local-SS extraction fixture
- bundled 5-curve / 1,245-row data and four metrics
- 728-window / 72-grid / 10-cutoff sensitivity reconstruction
- forbidden public artifacts, semantic metric recalculation and manifest logic

Result:
Ran 20 tests
OK

The release-level `python verify_release.py` command separately verifies the
final file manifest, public-bundle exclusions and four recalculated metric rows.

# Metadata

The processed-data step records simulation conditions, row counts, model-card
hashes, and raw-file hashes here.

`data_manifest.csv` is the machine-readable source-run record;
`simulation_conditions.md` is the human-readable v1.0 summary.
